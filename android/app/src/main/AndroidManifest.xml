<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Hand mode uses the front camera. It is requested at runtime, only after the
      player chooses hand mode and has read why (see src/camera). Touch mode
      never needs it, so every camera hardware feature below is optional:
      without android:required="false" the CAMERA permission would make Google
      Play hide the game from phones without a (back) camera or autofocus.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!--
      No INTERNET permission here: the game needs no network. Debug builds add it
      (src/debug) for Metro; release builds strip it (src/release), so the shipped
      app cannot open a network connection at all.
    -->

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true"
        android:screenOrientation="portrait"
        android:hardwareAccelerated="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
